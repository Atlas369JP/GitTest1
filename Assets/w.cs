using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class w : MonoBehaviour
{
    [SerializeField] float Animeter;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("wwww");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("草"); 
    }
}
